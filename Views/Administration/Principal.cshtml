@model GECP_Front_End_Static.Models.PrincipalViewModel

@{
    Layout = "~/Views/Shared/_Layout.cshtml";
    ViewBag.Title = "PRINCIPAL - Dr. Bharat J. Shah";
}
<div class="clearfix"></div>

<div class="page_title3 engg_title">
    <div class="container">

        <h1>
            Message of Principal 
            <div class="line"></div>
        </h1>

    </div>
</div>
<!-- end page title -->


<div class="clearfix margin_top2"></div>

<div class="clearfix margin-bottom3"></div>
<div class="container py-5">
    <div class="clearfix margin-bottom3"></div>
    <div class="row align-items-center">
        <!-- Principal Image -->
        <div class="col-md-4 text-center">
            <div class="principalimage">
                <img src="/images/Faculty/701.jpg" alt="Principal Photo" />
            </div>
            <h4 class="principalname">Dr. Bharat J. Shah</h4>
            <p class="principaldesignation">PRINCIPAL</p>
            <p class="principaldesignation">Government Engineering College, Patan</p>
        </div>

        <!-- Principal Message -->
        <div class="col-md-8">
            <div class="principalmessage">
                <h2 class="mb-3">Personal Details</h2>
                
                    <p style="text-align: justify; font-size: 1.1rem;">
                        <ul class="principalpd">
                            <li><strong>Name:</strong> @Model.Name</li>
                            <li><strong>Designation:</strong> @Model.Designation</li>
                            <li><strong>Qualification:</strong> @Model.Qualification</li>
                            <li><strong>Experience:</strong> @Model.Experience</li>
                            <li><strong>Date of Joining:</strong> @Model.DateOfJoining.ToString("dd-MM-yyyy")</li>
                            <li><strong>Area of Interest:</strong> @Model.AreaOfInterest</li>
                            <li><strong>Email:</strong> @Model.Email</li>
                        </ul>
                
                </p>
            </div>
        </div>
    </div>
</div>


<div class="content_fullwidth">

    <!-- Tab Navigation -->
    <ul class="tabs3">
        @*<li class="tab-link current" data-tab="tab-0">Principal's Message</li>*@
        @* <li class="tab-link" data-tab="tab-1">Personal Details</li> *@
        <li class="tab-link" data-tab="tab-5">Experience</li>
        <li class="tab-link" data-tab="tab-2">Educational Qualifications</li>
        <li class="tab-link" data-tab="tab-3">Publications Summary</li>
        <li class="tab-link" data-tab="tab-4">Selected Publications</li>
        <li class="tab-link" data-tab="tab-6">Achievements</li>
        <li class="tab-link" data-tab="tab-7">Book Publication</li>
        <li class="tab-link" data-tab="tab-8">Expert Talks</li>
        <li class="tab-link" data-tab="tab-9">Memberships</li>
    </ul>

    <!-- Tab Content -->
    <div class="tabs-content3">
        <!-- Tab 0: Message and Image -->
        <div id="tab-0" class="tab-content">
            <div style="display: flex; flex-wrap: wrap; gap: 30px; align-items: flex-start;">
				@foreach (var item in Model.PrincipalMessages)
                {
                    <div style="flex: 1 1 300px; max-width: 300px;">
                        <img src="@Url.Content(item.ImagePath)" alt="Principal Photo" style="width: 100%; border-radius: 10px; box-shadow: 0 0 10px rgba(0,0,0,0.1);" />
                    </div>
                    <div style="flex: 2 1 500px;">
                        <h3 style="margin-bottom: 15px;">Message from the Principal</h3>
                        <p style="font-size: 18px; text-align: justify; line-height: 1.6;">@Html.Raw(item.Message)</p>
                    </div>
                }
            </div>
        </div>

        <!-- Tab 1: Personal Details -->
        <div id="tab-1" class="tab-content">
            <ul>
                <li><strong>Name:</strong> @Model.Name</li>
                <li><strong>Designation:</strong> @Model.Designation</li>
                <li><strong>Qualification:</strong> @Model.Qualification</li>
                <li><strong>Experience:</strong> @Model.Experience</li>
                <li><strong>Date of Joining:</strong> @Model.DateOfJoining.ToString("dd-MM-yyyy")</li>
                <li><strong>Area of Interest:</strong> @Model.AreaOfInterest</li>
                <li><strong>Email:</strong> @Model.Email</li>
            </ul>
        </div>

        <!-- Tab 2: Educational Qualifications -->
        <div id="tab-2" class="tab-content">
            <table>
                <thead>
                    <tr>
                        <th>Qualification</th>
                        <th>University / Board</th>
                        <th>Year of Passing</th>
                        <th>Result</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var edu in Model.EducationalQualifications)
                    {
                        <tr>
                            <td>@edu.Qualification</td>
                            <td>@edu.University</td>
                            <td>@edu.Year</td>
                            <td>@edu.Result</td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>

        <!-- Tab 3: Publications Summary -->
        <div id="tab-3" class="tab-content">
            <ul>
                @foreach (var pub in Model.PublicationsSummary)
                {
                    <li><strong>@pub.Category:</strong> @pub.Count</li>
                }
            </ul>
        </div>

        <!-- Tab 4: Selected Publications -->
        <div id="tab-4" class="tab-content">
            <ul>
                @foreach (var sp in Model.SelectedPublications)
                {
                    <li><strong>@sp.Type:</strong> @sp.Title — <em>@sp.JournalOrConference</em></li>
                }
            </ul>
        </div>

        <!-- Tab 5: Experience -->
        <div id="tab-5" class="tab-content">
            <table>
                <thead>
                    <tr>
                        <th>Designation</th>
                        <th>From</th>
                        <th>To</th>
                        <th>Department</th>
                        <th>Duration</th>
                        <th>Place</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var exp in Model.Experiences)
                    {
                        <tr>
                            <td>@exp.Designation</td>
                            <td>@exp.DateOfJoining</td>
                            <td>@exp.DateOfLeave</td>
                            <td>@exp.Department</td>
                            <td>@exp.Duration</td>
                            <td>@exp.Place</td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>

        <!-- Tab 6: Achievements -->
        <div id="tab-6" class="tab-content">
            <ul>
                @foreach (var ach in Model.Achievements)
                {
                    <li>@ach</li>
                }
            </ul>
        </div>

        <!-- Tab 7: Book Publication -->
        <div id="tab-7" class="tab-content">
            <ul>
                <li><strong>Title:</strong> @Model.Book.Title</li>
                <li><strong>Book Code:</strong> @Model.Book.BookCode</li>
                <li><strong>University:</strong> @Model.Book.University</li>
                <li><strong>Branch:</strong> @Model.Book.Branch</li>
                <li><strong>Semester:</strong> @Model.Book.Semester</li>
                <li><strong>ISBN:</strong> @Model.Book.ISBN</li>
                <li><strong>Publisher:</strong> @Model.Book.Publisher</li>
                <li>
                    <strong>Contents:</strong>
                    <ul>
                        @foreach (var topic in Model.Book.ContentTopics)
                        {
                            <li>@topic</li>
                        }
                    </ul>
                </li>
            </ul>
        </div>

        <!-- Tab 8: Expert Talks -->
        <div id="tab-8" class="tab-content">
            <table>
                <thead>
                    <tr>
                        <th>Year</th>
                        <th>Subject</th>
                        <th>Place</th>
                        <th>Details</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var talk in Model.ExpertTalks)
                    {
                        <tr>
                            <td>@talk.Year</td>
                            <td>@talk.Subject</td>
                            <td>@talk.Place</td>
                            <td>@talk.Details</td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>

        <!-- Tab 9: Memberships -->
        <div id="tab-9" class="tab-content current">
            <ul>
                @foreach (var mem in Model.Memberships)
                {
                    <li>@mem</li>
                }
            </ul>
        </div>
    </div>
</div>
    <!-- end featured section 19 -->

    <a id="lnkPopup" href="#animatedModal" class="popup-trigger hidden" data-target="#popup"></a>
    <div id="popup" class="popup">
        <div id="pdfreader" style="height:600px">
        </div>
        <span class="popup-btn-close">&times;</span>
    </div>
<style>
    /* --- Global Reset for the Section --- */
    .content_fullwidth {
        padding: 30px;
        background-color: #f9f9f9;
    }

    /* --- Tab Navigation --- */
    .tabs3 {
        list-style: none;
        padding: 0;
        margin: 0 0 30px;
        display: flex;
        flex-wrap: wrap;
        border-bottom: 2px solid #ddd;
        background: #fff;
        border-radius: 8px 8px 0 0;
        box-shadow: 0 2px 6px rgba(0, 0, 0, 0.05);
    }

        .tabs3 li {
            padding: 12px 20px;
            margin-right: 2px;
            cursor: pointer;
            background: #f0f0f0;
            font-weight: 600;
            border-top-left-radius: 8px;
            border-top-right-radius: 8px;
            transition: all 0.3s ease;
            color: #333;
            user-select: none;
        }

            .tabs3 li:hover {
                background: #e0e0e0;
            }

            .tabs3 li.current {
                background: #ffffff;
                border: 2px solid #ddd;
                border-bottom: 2px solid #ffffff;
                color: #007bff;
            }

    /* --- Tab Content Container --- */
    .tabs-content3 {
        background: #ffffff;
        border: 2px solid #ddd;
        border-radius: 0 0 8px 8px;
        padding: 25px;
        box-shadow: 0 2px 8px rgba(0, 0, 0, 0.04);
    }

    .tab-content {
        display: none;
        animation: fadeIn 0.4s ease-in-out;
    }

        .tab-content.current {
            display: block;
        }

    /* --- Fade-in Animation --- */
    
    /* --- Principal Image and Message Layout --- */
    #tab-0 > div {
        display: flex;
        flex-wrap: wrap;
        gap: 30px;
        align-items: flex-start;
    }

    #tab-0 img {
        width: 100%;
        border-radius: 10px;
        box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
    }

    #tab-0 h3 {
        margin-bottom: 15px;
        color: #007bff;
        font-weight: 600;
    }

    #tab-0 p {
        font-size: 18px;
        line-height: 1.7;
        text-align: justify;
    }

    /* --- Tables --- */
    table {
        width: 100%;
        border-collapse: collapse;
        margin-top: 20px;
    }

        table th, table td {
            border: 1px solid #ddd;
            padding: 12px 15px;
            text-align: left;
        }

        table th {
            background-color: #f2f2f2;
            font-weight: bold;
            color: #333;
        }

    /* --- Lists --- */
    .tab-content ul {
        list-style-type: disc;
        padding-left: 25px;
        font-size: 16px;
    }

        .tab-content ul li {
            margin-bottom: 8px;
        }

    .principalimage img {
        max-height: 375px;
        border-radius: 50px;
        border: 3px solid #5076bb;
        box-shadow: 10px 10px 5px grey;
    }

    .principalimage {
        padding-top: 5%;
    }

    .principalname {
        font-size: 30px;
        line-height: 25px;
        margin-bottom: 20px;
        font-style: italic;
        font-family: emoji;
        font-weight: bold;
        padding-top: 10px;
    }

    .principaldesignation {
        font: 16px 'Open Sans', sans-serif;
        font-weight: bold;
        line-height: 23px;
    }

    .principalmessage {
        padding-top: 4%;
    }
    .principalpd{
        font-size:15px;
        line-height: 30px;
    }

    
</style>

@section tabs {

    <script src="@Url.Content("~/js/tabs2/index.js")"></script>
    <script>
        $('.accordion, .tabs').TabsAccordion({
            hashWatch: true,
            pauseMedia: true,
            responsiveSwitch: 'tablist',
            saveState: sessionStorage,
        });
    </script>
    <script>
        document.addEventListener("DOMContentLoaded", function () {
                    const tabs = document.querySelectorAll(".tab-link");
                    const contents = document.querySelectorAll(".tab-content");

                    tabs.forEach(tab => {
                        tab.addEventListener("click", function () {
                            const tabId = this.getAttribute("data-tab");

                            tabs.forEach(t => t.classList.remove("current"));
                            this.classList.add("current");

                            contents.forEach(c => c.classList.remove("current"));
                            document.getElementById(tabId).classList.add("current");
                        });
                    });
                });
    </script>
    <script defer src="@Url.Content("~/js/flexslider/jquery.flexslider.js")"></script>
    <script defer src="@Url.Content("~/js/flexslider/custom.js")"></script>
    @* <script type="text/javascript" src="@Url.Content("~/js/tabs/tabwidget/tabwidget.js")"></script> *@
    <script src="@Url.Content("~/js/tabs/assets/js/responsive-tabs.min.js")" type="text/javascript"></script>
}
    @section scripts{
        <script src="@Url.Content("~/js/pdfobject/pdfobject.min.js")"></script>
        <script>
                
            function loadFacultyinfo(url) {
                if (url !== '') {
                    $("#pdfreader").load(url);
                    $("#lnkPopup").click();
                }
            }

            function loadPdf(path, name) {
                if (path !== '') {
                    path = path.replace("~", "");
                    if (PDFObject.supportsPDFs) {
                        PDFObject.embed(path, "#pdfreader");
                        $("#newsname").text(name);
                        $("#lnkPopup").click();
                    } else {
                        window.location.href = path;
                    }
                }
            }
            function popupOpenClose(popup) {

                /* Add div inside popup for layout if one doesn't exist */
                if ($(".wrapper", popup).length == 0) {
                    $(popup).wrapInner("<div class='wrapper'></div>");
                }
                /* Open popup */
                $(popup).show();
                /* Close popup if user clicks on background */
                $(popup).click(function (e) {
                    if (e.target == this) {
                        if ($(popup).is(':visible')) {
                            $(popup).hide();
                            resetPopUpScroll();
                        }
                    }
                });
                /* Close popup and remove errors if user clicks on cancel or close buttons */
                $(popup).find(".popup-btn-close").on("click", function () {
                    if ($(".formElementError").is(':visible')) {
                        $(".formElementError").remove();
                    }
                    $(popup).hide();
                    resetPopUpScroll();
                });
            }

            $(document).ready(function () {
                $(".popup-trigger").on("click", function () {
                    var target = $(this).data('target');
                    popupOpenClose($(target));
                    resetPopUpScroll();
                });
            });

            function resetPopUpScroll() {
                $("#pdfreader").scrollTop(0);
            }
        </script>
    }